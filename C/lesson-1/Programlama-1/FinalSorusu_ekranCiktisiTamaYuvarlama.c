#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    float a= 1.51;
    printf("%.1f ",round(a));
    printf("%.1f ",floor(a));
    printf("%.1f ",ceil(a));
    printf("%.1f ",sqrt(81));
    printf("%.1f ",pow(2,4));
    return 0;
}
